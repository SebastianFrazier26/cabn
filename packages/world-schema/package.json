{
	"name": "@cabn/world-schema",
	"version": "0.0.0",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "vitest run",
		"gen:jsonschema": "pnpm run build && node scripts/gen-jsonschema.mjs"
	},
	"dependencies": {
		"zod": "^4.6.5"
	}
}
